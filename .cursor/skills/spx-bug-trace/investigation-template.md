# 问题排查过程文档模板

将此模板复制到 `docs/investigations/YYYYMMDD-<简短描述>.md` 使用。

---

# [问题标题]

| 字段 | 内容 |
|------|------|
| 日期 | YYYY-MM-DD |
| 报告人 | |
| 涉及系统 | 如：Hub Overview / Driver Overview 看板 |
| 涉及市场 | 如：ID, SG |
| 涉及站点 | 如：station_id = 166 |
| 状态 | 排查中 / 已定位 / 已修复 |

---

## 问题描述

[用一两句话描述用户反馈的现象，使用业务语言，不包含技术术语]

示例：Hub Overview 的 Driver Overview 看板在 ID 市场所有站点均显示无数据。

---

## 排查过程

### 步骤 1：[步骤标题]

**目的**：[此步骤要验证什么假设]

**操作**：

[描述执行了什么操作，如查询了哪张表]

**查询 SQL**：

```sql
-- 说明查询目的
SELECT ...
FROM ...
WHERE ...
```

**查询结果**：

| 列名 | 值 |
|------|----|
| ... | ... |

**结论**：[此步骤验证/排除了什么]

---

### 步骤 2：[步骤标题]

（重复上述格式）

---

## 根因分析

### 根因一：[标题]

[描述]

### 根因二（如有）：[标题]

[描述]

---

## 验证证据

### 证据 1：数据管道健康检查

```sql
SELECT 
    toDateTime(max(_process_time)) AS last_write_time,
    max(<date_column>)             AS last_data_date
FROM <table_name>
```

结果：

| last_write_time | last_data_date |
|-----------------|----------------|
| 实际值 | 实际值 |

### 证据 2：源表数据验证

```sql
SELECT 
    count(*)                               AS total_cnt,
    count(DISTINCT <driver_column>)        AS driver_cnt,
    -- 根据实际情况调整
FROM <source_table>
WHERE <partition_filter>
  AND <key_filter>
```

结果：

| total_cnt | driver_cnt | ... |
|-----------|------------|-----|
| 实际值 | 实际值 | ... |

---

## 结论（面向业务用户）

[用业务语言描述，分阶段说明（若有多阶段根因）。不暴露技术实现细节，如字段名、函数名等。]

示例格式：

**第一阶段（时间范围）：**

[基础设施层面的问题，用业务化语言描述，如"后台实时数据处理服务因资源不足崩溃，导致数据停止同步"]

**第二阶段（时间范围）：**

[业务逻辑或口径层面的问题，用业务语言描述]

经查询实时数据确认，您的站点数据完整：

| 指标 | 数值 |
|------|------|
| 今日司机数 | X 人 |
| 今日任务数 | X 条 |

---

## 修复建议

**短期（立即可用）：**

[绕过方案，如使用旧版看板]

**长期（需排期）：**

[根本修复方案，如产品侧评估口径扩展]

---

## 技术背景（供内部参考）

[此节面向技术人员，可包含技术细节：具体表名、字段名、Flink 任务名、错误日志摘要等]

**涉及表**：

- CK 表：`<table_name>`
- Presto 表：`<table_name>`

**涉及接口**：

- api_id：`<api_id>`
- biz_sql 关键逻辑：[摘要]

**动态参数说明**：

| 参数 | 传入值 | 影响 |
|------|--------|------|
| `{trip_zero_filter}` | `AND trip_id <> 0` | 过滤掉所有 trip_id=0 的任务 |

**错误日志摘要**（如有）：

```
[关键错误行，去掉重复堆栈，保留根因行]
```

---

## Skill 更新记录

本次排查完成后，对 `spx-bug-trace` Skill 进行了以下更新：

| 更新内容 | 原因 |
|----------|------|
| [描述改动] | [本次排查中遇到的问题] |

若无更新，填写"本次排查未发现 Skill 需更新的内容"。
