---
description: 文档同步规则：修改功能代码时必须同步更新对应文档
globs:
alwaysApply: true
---

# 文档同步规则

## 核心原则

**每次功能变更提交时，必须同步更新对应模块的说明文档。**

## 模块 ↔ 文档映射（顶层指南）

| 修改范围 | 必须更新的文档 |
|---------|---------------|
| `chrome-extension/` 下的代码 | `docs/guides/CHROME_EXTENSION.md` |
| `mcp-tools/` 下的 `.py`/`.json`/`.toml`/`.cfg` | `docs/guides/MCP_TOOLS.md` |
| `seatalk-agent/src/` 下的代码 | `docs/guides/SEATALK_AGENT.md` |
| `.cursor/skills/` 下的 Skill | `docs/guides/SKILL.md` |

## 子模块 ↔ 文档映射

修改 MCP 子模块代码时，除了更新顶层 `MCP_TOOLS.md`，还必须更新子模块自身的 README：

| 修改范围 | 必须更新的文档 |
|---------|---------------|
| `mcp-tools/<tool>/` 下的 `.py`/`.json`/`.toml`/`.cfg` | `mcp-tools/<tool>/README.md` |

例如：修改了 `mcp-tools/datamap-query/datamap_mcp_server.py`，需同时更新：
1. `mcp-tools/datamap-query/README.md`（工具详细说明）
2. `docs/guides/MCP_TOOLS.md`（顶层概览与配置示例）

## 发版日志

每次提交到 `release` 分支时（不论类型），必须在 `docs/RELEASE_LOG.md` 中追加记录。

## Hook 保障

`pre-push` hook 会自动检查上述映射关系，如果修改了模块代码但未更新对应文档，推送将被阻止。

## 文档目录结构

```
docs/
├── guides/
│   ├── CHROME_EXTENSION.md   # Chrome 扩展指南
│   ├── MCP_TOOLS.md          # MCP 工具指南（含推荐外部 MCP）
│   ├── SEATALK_AGENT.md      # SeaTalk Agent 指南
│   └── SKILL.md              # Cursor Skill 指南
└── RELEASE_LOG.md             # 发版日志
```
