---
description: SeaTalk Agent 开发规则。编辑 seatalk-agent/ 下的文件时自动激活。
globs: seatalk-agent/**
alwaysApply: false
---

# SeaTalk Agent 开发规则

## 架构

```
seatalk-agent/
├── src/
│   ├── main.ts              # 核心：CDP 连接、ACP 编排、自动更新
│   ├── inject/
│   │   ├── sidebar-app.js   # 前端面板 UI（注入 SeaTalk 页面）
│   │   └── cursor-ui.js     # UI 基础组件
│   └── ...
├── install.sh               # 一键安装（LaunchAgent + CDP 守护进程）
├── launch.sh                # 启动脚本
├── seatalk-cdp-daemon.sh    # CDP 守护进程（确保 SeaTalk 以 CDP 模式运行）
└── package.json             # 版本号必须与 manifest.json 一致
```

## 关键路径

| 常量 | 值 | 说明 |
|------|---|------|
| `PROJECT_ROOT` | `path.resolve(__dirname, '../..')` | 项目根目录（SPX_Helper/） |
| `CDP_PORT` | `19222` | SeaTalk CDP 调试端口 |
| `WORKSPACE` | 用户选择的 Cursor 工作区 | Agent 执行命令的工作目录 |

## 高风险文件

| 文件 | 修改后必须 |
|------|-----------|
| `src/main.ts` | 重启 Agent (`seatalk`) |
| `src/inject/sidebar-app.js` | 重启 Agent + CDP 测试验证 |
| `install.sh` | 重新执行 `bash install.sh` |
| `seatalk-cdp-daemon.sh` | 重新加载 LaunchAgent |

## 自动更新机制

- 基于 `chrome-extension/manifest.json` 的 version 对比
- `git fetch origin release` → 对比本地 vs 远程 manifest version
- 版本不一致 + commit 落后 → 前端显示更新通知
- `silentUpdateCheck` 无条件发送版本状态给前端

## 开发注意事项

- 修改 `inject/` 下的 JS 文件后，必须通过 CDP 测试验证（参见 `seatalk-agent-testing.mdc`）
- `install.sh` 修改后需覆盖测试：安装、卸载、重装
- CDP 守护进程日志位于 `~/.seatalk-agent/logs/cdp-daemon.log`
- Agent 进程日志位于 `~/.seatalk-agent/logs/agent.log`

## 常用命令

```bash
seatalk                          # 一键启动（alias）
npx tsx src/main.ts              # 开发模式启动
bash install.sh                  # 安装
bash install.sh uninstall        # 卸载
curl -s http://127.0.0.1:19222/json  # 验证 CDP 可用
```
