---
description: Git 提交规范、分支管理、版本管理、双仓库同步。编辑任何文件准备提交时适用。
globs:
alwaysApply: true
---

# Git 工作流与提交规范

## 版本管理

### 唯一版本源
`chrome-extension/manifest.json` 的 `version` 字段是项目唯一版本号来源。

| 文件 | 规则 |
|------|------|
| `chrome-extension/manifest.json` | **主版本源**，发版时只改这里 |
| `package.json`（根） | 跟随 manifest.json 同步 |
| `seatalk-agent/package.json` | 跟随 manifest.json 同步 |

### 何时必须升版本号

提交包含以下任一变更时，**必须升版本号**：
- `feat`: 新功能（MCP 工具、Chrome 扩展功能、SeaTalk Agent 功能）
- `fix`: Bug 修复
- `refactor`: 影响用户使用的重构（如项目结构变更）

以下提交**不需要**升版本号：
- `docs`: 纯文档更新
- `style`: 代码格式调整
- `chore`: 构建/工具链变更

### 版本号规则（SemVer）
- `MAJOR.MINOR.PATCH`（如 3.1.0）
- MAJOR: 不兼容的架构变更
- MINOR: 新功能、重要修复（如 3.0.3 → 3.1.0）
- PATCH: 小修复、小优化（如 3.1.0 → 3.1.1）

### 发版流程
```
1. 改 chrome-extension/manifest.json 的 version
2. 同步更新 package.json（根）的 version
3. 同步更新 seatalk-agent/package.json 的 version
4. 更新 README.md 的 Changelog
5. 提交（三个 version 必须一致）
```

### 禁止
- ❌ feat/fix 提交不升版本号
- ❌ 只改其中一个文件的版本号
- ❌ 各文件版本号不一致
- ❌ 用 package.json 作为版本判断来源

## 提交规则

### Agent 禁止行为
- ❌ 自动提交代码到 Git（必须等用户说"可以提交"）
- ❌ 未经测试执行 `git commit`
- ❌ 未经确认执行 `git push`
- ❌ 只推送到单个远程仓库
- ❌ 未经用户确认直接推送

### 提交流程
```
接收需求 → 分析问题 → 修改代码 → 展示修改内容(git diff)
  → 🛑 等待用户测试
  → 🛑 等待用户确认"可以提交"
  → git add + git commit
  → git push gitlab <branch> && git push origin <branch>
```

### Commit Message 格式
```
<type>: <subject>

<body>
```

type: `feat` | `fix` | `docs` | `style` | `refactor` | `perf` | `test` | `chore`

## 分支管理

| 分支 | 用途 |
|------|------|
| `feature/*` | 新功能，从 release 拉出 |
| `bugfix/*` | Bug 修复，从 release 拉出 |
| `release` | 开发主分支，可直接 push |
| `main` | 稳定版本，从 release 合并 |

流程: 日常开发直接在 `release` 上提交推送；大功能可用 `feature/bugfix` 分支开发后合入 `release`

## 双仓库同步

```
GitLab: gitlab  https://git.garena.com/tianyi.liang/spx-helper.git
GitHub: origin  https://github.com/SperanzaTY/spx-helper.git

推送时必须同时推送到两个仓库:
git push gitlab <branch> && git push origin <branch>
```

## 提交前检查清单

- [ ] 修改的功能正常工作
- [ ] 已有功能未被破坏
- [ ] commit message 符合规范
- [ ] **feat/fix 类型提交必须升版本号**（三个文件同步）
- [ ] 若涉及功能变更，对应模块 README 已更新
- [ ] 若涉及功能变更，用户指南已更新
