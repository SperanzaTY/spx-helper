---
description: Git 提交规范、分支管理、版本管理、双仓库管理。编辑任何文件准备提交时适用。
globs:
alwaysApply: true
---

# Git 工作流与提交规范

## 版本管理

### 唯一版本源
`chrome-extension/manifest.json` 的 `version` 字段是项目唯一版本号来源。

| 文件 | 规则 |
|------|------|
| `chrome-extension/manifest.json` | **主版本源**，发版时只改这里 |
| `package.json`（根） | 跟随 manifest.json 同步 |
| `seatalk-agent/package.json` | 跟随 manifest.json 同步 |

### 何时必须升版本号

提交包含以下任一变更时，**必须升版本号**：
- `feat`: 新功能（MCP 工具、Chrome 扩展功能、SeaTalk Agent 功能）
- `fix`: Bug 修复
- `refactor`: 影响用户使用的重构（如项目结构变更）

以下提交**不需要**升版本号：
- `docs`: 纯文档更新
- `style`: 代码格式调整
- `chore`: 构建/工具链变更

### 版本号规则（SemVer）
- 格式：`MAJOR.MINOR.PATCH`（如 3.2.4）
- **MAJOR**：不兼容的架构变更（用户需重新安装/配置）
- **MINOR**：跨模块的大型功能或大规模重构（如新增整个 MCP 工具、SeaTalk Agent 全新模块），需用户明确要求升 MINOR
- **PATCH**：日常开发的默认升级级别，包括：新功能（feat）、Bug 修复（fix）、重构（refactor）

> ⚠️ **重要**：绝大多数提交都只升 PATCH。`feat` 类型默认也升 PATCH，不要自动升 MINOR。
> 只有在用户明确指示"升小版本/升 MINOR"时才升 MINOR。
>
> 举例：当前版本 3.2.3
> - `feat: 添加权限控制` → **3.2.4**（PATCH +1，feat 默认升 PATCH）
> - `fix: 修复重启问题` → **3.2.4**（PATCH +1）
> - 用户说"升小版本" → **3.3.0**（MINOR +1，PATCH 归零）

### 发版流程（Agent 自动执行）
```
1. 🤖 判断 commit type → feat/fix/refactor 时升版本号（默认 PATCH +1，三个文件同步）
2. 🤖 更新 README.md 的 Changelog（feat/fix/refactor 时）
3. 🤖 在 docs/RELEASE_LOG.md 中生成本次提交的记录（所有 commit type 都必须！）
4. 🤖 生成 .release-notification 群通知文件（见下方说明）
5. 🤖 提交（三个 version 必须一致）
```

> ⚠️ RELEASE_LOG 是**每次提交**都必须写的，不仅限于升版本号的提交。
> pre-push hook 检查最新 commit 是否修改了 RELEASE_LOG.md，未修改则阻止推送。

> ⚠️ 修改了 SeaTalk Agent 相关代码后，必须重启 Agent 测试（终端运行 `seatalk` 或用面板内更新功能）。
> 其他团队成员拉取代码后也需要重启 Agent 才能生效。

### 禁止
- ❌ feat/fix 提交不升版本号
- ❌ 只改其中一个文件的版本号
- ❌ 各文件版本号不一致
- ❌ 用 package.json 作为版本判断来源
- ❌ **未推送中间发版**就在工作区把 manifest **连续改多个 PATCH**（例如远程仍是 `3.5.20` 时本地直接改成 `3.5.22`）。每个发版 **相对主远程 `release` 上 manifest 版本最多「跳一档」**：同 `MAJOR.MINOR` 时 `PATCH` 至多 `+1`，或 `MINOR` 至多 `+1`，或 `MAJOR` 至多 `+1`（禁止跨档跳变）。由 **`.githooks/lib/version-step.sh`** 实现，**`.githooks/pre-push`** 在推送 `release` 时必检；**`.githooks/pre-commit`** 在 **任意分支上只要暂存 `chrome-extension/manifest.json`** 即校验（基准远程：`SPX_RELEASE_VERSION_REMOTE` 环境变量，否则优先 `gitlab`、否则 `origin`，会先 `git fetch <remote> release`）。

## 提交规则

### Agent 禁止行为
- ❌ 自动提交代码到 Git（必须等用户说"可以提交"）
- ❌ 未经测试执行 `git commit`
- ❌ 未经确认执行 `git push`
- ❌ 未经用户确认直接推送

### 提交流程（Agent 执行，用户审核）

Agent 在用户确认"可以提交"后，**自动依次执行**以下步骤（无需用户逐项操作）：

```
1. 🤖 判断 commit type → 如果是 feat/fix/refactor 自动升版本号（三个文件同步）
2. 🤖 自动读取 git diff，在 docs/RELEASE_LOG.md 中追加本次提交的记录（所有类型！）
3. 🤖 检查三个文件版本号一致性
4. 🤖 生成 .release-notification 群通知文件（见"群通知"章节）
5. 🤖 若涉及 SeaTalk Agent 修改，提醒用户需要重启 Agent 测试
6. 🤖 展示所有变更摘要 → 🛑 等待用户最终确认
7. 🤖 git add + git commit + `npm run push:release`（或 `bash scripts/push-release.sh`；GitLab 推送成功后再同步 GitHub 并投递 `.release-notification`）
```

> 用户只需在第 6 步做最终审核确认，其余均由 Agent 自动完成。
> **重点**：第 2 步对所有 commit type 都执行，不只是升版本号的提交。

### ⚠️ Agent 自动检查项（commit 前必须全部执行）

Agent 在 `git commit` 前**必须自动验证**（不需要问用户，发现问题直接修复）：

0. **远程同步**：执行 `git fetch gitlab release`，然后 `git rev-list --count HEAD..gitlab/release` 检查是否落后。如果落后，先 `git pull --rebase gitlab release` 再继续
0.5. **Git hooks 已安装**：在项目根目录执行 `bash scripts/verify-hooks.sh` 或 `npm run verify:hooks`。若失败（`core.hooksPath` 未指向本仓库 `.githooks/`），先执行 `npm run setup` 或 `git config core.hooksPath .githooks` 并 `chmod +x .githooks/*`，再重试直至通过。**禁止**在未通过校验的情况下继续 `git commit` / `git push`，否则 pre-push 等检查不会生效，容易漏发版日志、版本不一致等问题。
1. **版本号**：commit type 是 feat/fix/refactor？→ 默认升 PATCH 版本（feat 也只升 PATCH！），三个文件同步
2. **版本一致性**：读取三个文件的 version 字段，不一致则自动修正
3. **发版日志（每次提交必须！）**：自动在 `docs/RELEASE_LOG.md` 中追加本次提交的记录（基于 git diff 生成变更说明 + 测试项）。不管是 feat/fix 还是 docs/chore，**每次提交都必须有记录**。pre-push hook 检查最新 commit 是否修改了 RELEASE_LOG.md
4. **README Changelog**：如果是 feat/fix/refactor，自动在 README.md 更新日志中追加条目
5. **群通知文件**：生成 `.release-notification` 文件（见下方"群通知"章节）；在 **GitLab 推送成功之后** 由 `scripts/finish-release-push.sh` 调用 `notify-release.sh` 投递（推荐经 `npm run push:release` 一条完成）
6. **SeaTalk Agent 重启提醒**：如果修改了 `seatalk-agent/` 下的文件，在提交摘要中提醒用户重启 Agent

### Commit Message 格式
```
<type>: <subject>

<body>
```

type: `feat` | `fix` | `docs` | `style` | `refactor` | `perf` | `test` | `chore`

## 分支管理

| 分支 | 用途 |
|------|------|
| `feature/*` | 新功能，从 release 拉出 |
| `bugfix/*` | Bug 修复，从 release 拉出 |
| `release` | 开发主分支，可直接 push |
| `main` | 稳定版本，**只能通过 MR 从 release 合入**，pre-push hook 会阻止直接推送 |

流程: 日常开发直接在 `release` 上提交推送；大功能可用 `feature/bugfix` 分支开发后合入 `release`

## 双仓库管理

```
GitLab: gitlab  https://git.garena.com/tianyi.liang/spx-helper.git  ← 主仓库，检查基准
GitHub: origin  https://github.com/SperanzaTY/spx-helper.git       ← 自动同步，失败不阻塞

推送命令（release 发版推荐）：
npm run push:release
# 或: bash scripts/push-release.sh

pre-push 只做校验；**不在 hook 内**同步 GitHub 或发群通知（pre-push 返回时 Git 尚未完成向 GitLab 的传输）。
GitLab 推送成功后的 GitHub 同步与 SeaTalk 通知由 `scripts/finish-release-push.sh` 执行；GitHub 失败不阻塞，owner 可后续同步。
```

## Git Hooks（项目级）

项目使用 `.githooks/` 目录存放 hook 脚本，通过 `core.hooksPath` 生效。

| Hook | 作用 |
|------|------|
| `pre-push` | 推送到 release 前多项校验（远程、RELEASE_LOG、版本号、模块文档、feat/fix 升版、Commit 格式、Agent 变更时 CDP 等）；**不**执行 GitHub 同步或 SeaTalk 通知；通过后提示使用 `npm run push:release` |

### 首次使用 / 拉取含 hook 变更后
```bash
git config core.hooksPath .githooks
# 或运行 npm run setup
```

> 根目录 `npm install` 的 `prepare` 脚本会执行 `setup-hooks.sh` 自动配置。

### 校验是否已安装（提交前必跑）
```bash
npm run verify:hooks
# 或: bash scripts/verify-hooks.sh
```
若报错，说明本机未指向项目 `.githooks/`，推送时 **不会** 跑 pre-push 等检查。先 `npm run setup` 再提交。

**常见原因**：`.githooks/` 在仓库中更新后，若未重新执行 `npm run setup`，`core.hooksPath` 仍可能指向旧目录或未设置。

## 提交前检查清单

### 🤖 Agent 自动执行（无需用户操作）
- [ ] **远程同步检查**：git fetch + 检查是否落后，落后则先 pull --rebase
- [ ] **Git hooks 校验**：`npm run verify:hooks` 通过；失败则 `npm run setup` 后重试
- [ ] 版本号升级（feat/fix/refactor 时默认 PATCH +1，三个文件同步；feat 也只升 PATCH，除非用户明确要求升 MINOR）
- [ ] **docs/RELEASE_LOG.md 中追加本次提交的记录（每次提交必须！hook 检查 RELEASE_LOG 是否被修改）**
- [ ] README.md Changelog 追加条目（feat/fix/refactor 时）
- [ ] commit message 符合 `<type>: <subject>` 规范
- [ ] 涉及 SeaTalk Agent 修改时，在提交摘要中提醒重启

### 👤 用户辅助确认（Agent 展示后用户过目即可）
- [ ] 修改的功能正常工作
- [ ] 已有功能未被破坏
- [ ] 版本号和发版日志内容正确

## 群通知（Release Notification）

在 **GitLab 推送成功之后**，`scripts/finish-release-push.sh` 会调用 `scripts/notify-release.sh`，将 `.release-notification` 的内容 POST 到 SeaTalk 问题反馈群（推荐与推送同条链路：`npm run push:release`）。

### AI 生成规则

Agent 在 `git commit` 前，必须生成 `.release-notification` 文件（该文件已 gitignore，不会被提交）。

**内容要求**：
- 由 AI 根据本次变更**用自然语言撰写**，不是机械模板
- 简洁易读，面向团队成员，不是面向开发者
- 时间精确到分钟
- 末尾标注"自动发布通知"

**必须包含的信息**：
1. 版本号
2. 本次变更的核心内容（1-5 条，用人话说）
3. 是否需要重启 Agent / 重新加载扩展 / 其他操作
4. 特别注意事项（如有）

**参考格式**（AI 可根据实际内容灵活调整）：

```
[SPX Helper v3.4.13 Released]

本次更新:
- SeaTalk Agent 连接方式重构，解决部分用户 CDP 连不上的问题
- 不再依赖 --remote-debugging-port 启动参数
- 删除了 CDP 守护进程

[!] 需要操作:
- 所有用户需要重启 Agent（终端运行 seatalk 或在面板点更新）
- 已安装守护进程的用户建议重新运行 install.sh

---
自动发布通知 | 2026-04-08 16:30
```

**写入方式**：
```bash
# Agent 在 commit 前通过 Write 工具写入项目根目录
# 文件路径: <项目根目录>/.release-notification
```

### 投递机制

- `scripts/push-release.sh`：`git push gitlab release` 成功后调用 `scripts/finish-release-push.sh`
- `finish-release-push.sh`：同步 `origin/release`（失败仅告警），若存在 `.release-notification` 则调用 `scripts/notify-release.sh`
- `notify-release.sh`：读取文件，POST 到 SeaTalk webhook；成功则删除文件
- 发送失败不阻塞本地仓库状态，文件保留供手动重试：`bash scripts/notify-release.sh`
