---
description: MCP 工具开发规范与各工具使用说明。编辑 mcp-tools/ 下的文件时自动激活。
globs: mcp-tools/**
alwaysApply: false
---

# MCP 工具开发规范

## 开发规范

### 目录结构
每个 MCP 工具独立目录 `mcp-tools/<tool-name>/`，必须包含：

```
<tool-name>/
├── <tool>_mcp_server.py   # 主程序（或 *_server.py）
├── pyproject.toml          # 包定义（uvx 安装需要）
└── README.md               # 工具说明（必须！）
```

### 编码规范
- 使用 `FastMCP` 框架，`transport="stdio"`
- 凭证通过环境变量传入，**禁止硬编码**
- 所有网络请求必须 try-catch，返回有意义的错误信息
- 工具函数的 docstring 就是用户在 Cursor 里看到的工具描述，必须清晰

### 认证规范（Cookie 类 MCP）
依赖 Chrome Cookie 的 MCP **必须使用 `get_auth()`**，禁止直接用 `get_cookies()`：

```python
# [CORRECT] 磁盘 Cookie 不够时自动走 CDP 实时获取
from chrome_auth import get_auth

def _load_cookies(force=False):
    result = get_auth(domain, force=force)  # cookie_db -> cdp_cookie 自动降级
    return result.cookies

# [WRONG] 只读磁盘，Cookie 过期/未刷盘就 401
from chrome_auth import get_cookies
cookies = get_cookies(domain=domain)
```

`get_auth()` 默认策略 `["cookie_db", "cdp_cookie"]`：**合并**磁盘与 CDP 两套 Cookie（同名以 CDP 为准），并修正对父域（如 `.shopee.io`）Cookie 的过滤。macOS 新版 Chrome 下 DataSuite 的 `DATA-SUITE-AUTH-userToken-v4` 可能加密存储、磁盘读不到，须用 **Chrome `--remote-debugging-port=9222`** 让 CDP 取全（见 `mcp-tools/chrome-auth/scripts/start_chrome_remote_debug.sh` 与 `cookie_diagnostic`）。

适用模块：`scheduler-query`、`datamap-query` 等所有通过 `chrome_auth` 获取 DataSuite/Keyhole Cookie 的 MCP。

### 本机开发时的 Cursor 配置
仓库贡献者应使用**本地 `mcp-tools/<tool>/` 路径**配置 `~/.cursor/mcp.json`（`python3` + 入口脚本 + `PYTHONPATH` 含 `chrome-auth` 等），与「仅从 release 拉 uvx」相比，改代码立即生效。详见 `docs/guides/MCP_TOOLS.md` 中「本地仓库（开发者推荐）」。

### 修改后刷新
MCP 工具代码修改后，需要在 Cursor Settings → MCP 中**关闭再开启**对应服务以刷新。

### README 维护规则
- **新增 MCP 工具**：必须在工具目录下创建 `README.md`，包含功能说明、配置方式、工具列表、使用示例
- **修改 MCP 工具**：若新增/删除/重命名了工具函数，必须同步更新该工具的 `README.md`
- **同时更新** `mcp-tools/README.md` 工具列表

### 验证注册
```bash
python3 -c "import asyncio; from <module> import mcp; asyncio.run(mcp.list_tools())"
```

---

## 工具清单与使用说明

### presto-query — Presto SQL 查询
- **用途**：通过 DataSuite API 查询 Presto 数据库
- **认证**：环境变量 `PRESTO_PERSONAL_TOKEN` + `PRESTO_USERNAME`
- **凭证获取**：https://datasuite.shopee.io/dataservice/ds_api_management → ☰ → Personal Token
- **核心工具**：
  - `query_presto(sql, queue?, region?, cell_max_len?, write_full_result_to?)` — 执行 Presto SQL
- **特性**：支持 `write_full_result_to` 将大结果集落盘为 JSON 文件
- **详细文档**：`mcp-tools/presto-query/README.md`

### spark-query — Spark SQL 查询
- **用途**：通过 Livy REST API 提交 Spark SQL
- **认证**：环境变量 `LIVY_USERNAME` + `LIVY_PASSWORD`
- **凭证获取**：[DataSuite RAM Profile](https://datasuite.shopee.io/ram/personal/profile) → BigData Account
- **核心工具**：
  - `spark_sql(sql, session_name?)` — 执行 Spark SQL
  - `list_sessions()` — 列出活跃 Livy Session
- **详细文档**：`mcp-tools/spark-query/README.md`

### ck-query — ClickHouse 查询
- **用途**：通过 HTTP 直连（DBeaver 方式）查询 ClickHouse 集群
- **认证**：密码内置于代码中（团队统一管理）
- **核心工具**：
  - `query_clickhouse(sql, cluster?, database?, max_rows?)` — 执行 CK SQL
- **可用集群**：`test`(spx_mart_pub), `ddc`, `ck2`/`online_2`, `ck6`/`online_6`, `online_4`(ck2读), `online_5`, `online_7`(ck6读)
- **注意**：默认集群为 `test`，写集群操作请谨慎

### api-trace — API 血缘溯源
- **用途**：查询 API 接口的数据血缘关系，从 spx_mart.api_lineage 表检索
- **认证**：同 presto-query（`PRESTO_PERSONAL_TOKEN` + `PRESTO_USERNAME`）
- **核心工具**：
  - `trace_api(api_path?, keyword?)` — 查询 API 的数据来源链路
- **详细文档**：`mcp-tools/api-trace/README.md`

### seatalk-reader — SeaTalk 消息读取
- **用途**：通过 CDP 协议读取 SeaTalk 桌面客户端的消息（Redux store + SQLite）
- **前提**：SeaTalk 以 CDP 模式运行（安装 SeaTalk Agent 后自动满足）
- **认证**：无需凭证，通过 CDP 本地连接
- **核心工具**：
  - `list_seatalk_chats(chat_type?)` — 列出聊天会话
  - `read_seatalk_messages(session_type?, session_name, limit?)` — 读取指定群/私聊消息
  - `search_seatalk_messages(keyword)` — 跨群搜索消息
  - `navigate_to_chat(session_name)` — 导航到指定聊天
  - `read_current_chat(limit?)` — 读取当前聊天窗口
  - `query_messages_sqlite(...)` — 从本地 SQLite 查历史消息
  - `query_mentions(hours?)` — 查询最近被 @ 的消息
  - `seatalk_screenshot()` — 截图
  - `seatalk_eval(code)` — 执行 JS
  - `open_seatalk_link(url)` — 打开链接
  - `download_seatalk_image(image_url)` — 下载图片
- **详细文档**：`mcp-tools/seatalk-reader/README.md`

### datamap-query — DataMap 表元数据查询 + 写入
- **用途**：查询和修改 DataMap 中的表/字段元数据
- **认证**：查询用 `get_auth()` 获取 Cookie；写入用 `DATAMAP_OPEN_API_TOKEN` 环境变量（Basic Auth）
- **查询工具**：
  - `get_table_info(table_ref, engine?)` — 表基本信息
  - `get_table_detail(table_ref, engine?)` — 表扩展详情（含字段列表）
  - `get_column_detail(table_ref, column_name, engine?)` — 单列详情
  - `get_partition_columns(table_ref, engine?)` — 分区字段
  - `get_table_usage(table_ref, engine?)` — 使用统计
  - `get_table_lineage(table_ref, engine?)` — 数据血缘
  - `get_table_score(table_ref, score_type?, engine?)` — 质量评分
  - `get_table_sla(table_ref, engine?)` — SLA 信息
  - `list_schemas(engine?)` — 列出所有 schema
  - `list_tables(database, engine?)` — 列出表
  - `search_tables(keyword, engine?)` — 搜索表
  - `search_global(keyword)` — 全局搜索
  - `get_table_audit_log(table_ref, engine?, page_size?)` — 审计日志
- **写入工具**（需配置 Open API Token）：
  - `update_table_info(table_ref, description?, technical_pic?, business_pic?, data_warehouse_layer?, market_region?, dry_run?)` — 更新表元数据
  - `update_column_info(table_ref, column_name, description?, calculation_logic?, enumeration?, biz_primary_key?, dry_run?)` — 更新字段元数据
- **写入安全**：默认 dry_run=True（仅预览变更），设为 False 执行实际更新
- **详细文档**：`mcp-tools/datamap-query/README.md`

### scheduler-query — DataSuite Scheduler 查询 + Spark 诊断
- **用途**：查询 Scheduler 任务状态、实例列表、运行日志、血缘、Presto/Spark SQL、Spark 性能诊断
- **认证**：通过 `get_auth()` 自动获取 Cookie（磁盘优先，失败走 CDP）
- **安装**：`pip3 install mcp requests browser-cookie3`
- **Scheduler 工具**：
  - `search_tasks(keyword, project_code?, env?)` — 搜索任务
  - `get_task_info(task_code, env?)` — 获取任务详情
  - `get_task_instances(task_code, env?, date?, page_size?)` — 查询实例列表
  - `get_instance_detail(task_instance_code, env?)` — 查询实例详情（含 YARN App ID / Presto Query ID）
  - `get_instance_log(task_instance_code, env?)` — 查询运行日志
  - `parse_mart_sla_alert(alert_text)` — 解析新 Mart SLA 告警正文（本地）
  - `resolve_mart_sla_shortlink(shp_url)` — 将 shp.ee 短链解析为 DataSuite SLA 详情页 URL（HEAD Location，无需 Cookie）
  - `fetch_mart_sla_instances_from_shortlink(shp_url, env?)` — 短链 + Cookie 调 `GET /sla/slaInstance/get`，抽取 taskInstanceCode
  - `triage_mart_sla_alert(alert_text, env?, max_instances?, deep_spark?, resolve_shortlinks?, sla_shortlink_fetch_instances?)` — SLA 告警分诊（默认可用 slaInstance/get 从短链补全实例编码；解析 + 实例详情/日志，可选 Spark 摘要）
  - `get_task_lineage(task_code, env?)` — 查询上下游血缘
  - `get_task_metric_summary(task_code, env?)` — 运行指标汇总
  - `get_task_operation_log(task_code, days?, env?)` — 操作日志
  - `get_task_violations(task_code, project_code?, env?)` — SLA 违规记录
- **Presto 工具**：
  - `get_presto_query_sql(presto_query_id?, task_instance_code?, env?)` — 获取 Presto 任务的 SQL
- **Spark 诊断工具**（通过 Spark History Server REST API）：
  - `get_spark_query_sql(yarn_application_id?, task_instance_code?, env?)` — 获取 Spark 任务的 SQL（Keyhole 日志，回退 SHS）
  - `get_spark_app_summary(yarn_application_id?, task_instance_code?, env?)` — 应用概览 + 关键 Spark 配置
  - `get_spark_stages(yarn_application_id?, task_instance_code?, env?)` — Stage 指标 + 数据倾斜检测
  - `get_spark_executors(yarn_application_id?, task_instance_code?, env?)` — Executor 资源 + GC/OOM 检测
  - `get_spark_sql_plan(yarn_application_id?, task_instance_code?, env?)` — SQL 执行计划 + 算子指标
  - `get_spark_jobs(yarn_application_id?, task_instance_code?, env?)` — Job 列表及状态
  - `get_spark_stage_tasks(yarn_application_id?, task_instance_code?, stage_id, sort_by?, limit?, env?)` — Task 级明细 + 倾斜精确检测
  - `get_spark_storage(yarn_application_id?, task_instance_code?, env?)` — RDD/缓存信息
  - `diagnose_spark_app(yarn_application_id?, task_instance_code?, env?)` — 综合性能诊断（推荐入口，含 Task 级倾斜分析）
- **注意**：`env` 参数支持 `prod`/`dev`/`staging`，默认 `prod`
- **详细文档**：`mcp-tools/scheduler-query/README.md`

### seatalk-group — SeaTalk 批量拉群
- **用途**：通过 InfraBot API 批量邀请成员进 SeaTalk 群
- **前提**：群中已添加 InfraBot 并设为管理员
- **认证**：环境变量 `INFRABOT_TOKEN`
- **凭证获取**：https://space.shopee.io/utility/seatalkbot/api-playground → Get API Token
- **核心工具**：
  - `invite_to_group(group_id, emails, token?)` — 批量邀请成员
  - `check_group_setup(group_id, token?)` — 检查群 InfraBot 配置
- **详细文档**：`mcp-tools/seatalk-group/README.md`

---

## 配置位置

用户的 MCP 配置在 `~/.cursor/mcp.json`，完整模板见 `INSTALL.md` 模块 A。

## 内部平台文档查阅

对接公司内部平台开发前，先用 Confluence MCP 搜索相关文档：
- 搜索 `DI` 空间：`confluence_search(query="平台名 + API")`
- 已知文档：Scheduler Open API (page_id: 1382956585)
