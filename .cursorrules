# SPX Helper 项目开发规则

> 详细规则已按模块拆分到 `.cursor/rules/*.mdc`，Cursor 会根据当前编辑的文件自动加载对应规则。

## 规则索引

| 规则文件 | 作用域 | 内容 |
|---------|--------|------|
| `general.mdc` | 全局 | 项目介绍、架构概览、安装引导、安全约束 |
| `git-workflow.mdc` | 全局 | 提交规范、分支管理、版本管理、双仓库管理 |
| `doc-sync.mdc` | 全局 | 文档同步规则（修改功能必须更新对应 README） |
| `nopua.mdc` | 全局 | 问题解决方法论（卡住时的升维策略） |
| `chrome-extension.mdc` | `chrome-extension/**` | Chrome 扩展开发规则、文件职责 |
| `mcp-tools.mdc` | `mcp-tools/**` | MCP 工具开发规范、各工具使用说明 |
| `seatalk-agent.mdc` | `seatalk-agent/**` | SeaTalk Agent 开发规则、架构说明 |
| `seatalk-agent-testing.mdc` | `seatalk-agent/src/inject/*.js` | 前端注入代码 CDP 测试规则 |

## 核心约束（始终生效）

1. **禁止自动提交**：必须等用户说"可以提交"后才能 `git commit/push`
2. **推送策略**：`git push gitlab release`（检查以 GitLab 为基准，推送成功后自动尝试同步 GitHub，失败不阻塞）
3. **版本唯一源**：`chrome-extension/manifest.json` 的 version
4. **文档同步**：功能变更必须更新对应模块的 README
5. **凭证安全**：禁止硬编码密码/token，使用环境变量
6. **版本升级默认 PATCH**：`feat`/`fix`/`refactor` 均默认只升 PATCH（如 3.2.3 → 3.2.4）。只有用户明确要求时才升 MINOR（如 3.2.3 → 3.3.0）

## Skill

Skill 源文件在 `.cursor/skills/`，提交到 Git 供团队共享。
其他项目使用：`cp -r .cursor/skills/spx-bug-trace ~/.cursor/skills/`
详见 `docs/guides/SKILL.md`
