# SPX Helper 项目开发规则

## 📌 版本管理规范（重要！）

### 唯一版本源
- **`manifest.json` 的 `version` 字段是项目的唯一版本号来源**
- 所有 `package.json` 的 `version` 必须与 `manifest.json` 保持一致

### 版本文件清单
| 文件 | 用途 | 规则 |
|------|------|------|
| `chrome-extension/manifest.json` | Chrome 扩展版本声明 | **主版本源**，发版时只改这里 |
| `package.json`（根） | npm 包元数据 | 跟随 `manifest.json` 同步 |
| `seatalk-agent/package.json` | seatalk-agent 子模块 | 跟随 `manifest.json` 同步 |

### 发版流程
```
1. 修改 chrome-extension/manifest.json 的 version（如 2.18.4 → 2.19.0）
2. 同步更新 package.json（根）的 version
3. 同步更新 seatalk-agent/package.json 的 version
4. 更新 README.md 的版本号和 Changelog
5. 更新用户指南 docs/guides/SEATALK_AGENT_USER_GUIDE.md（见下方规则）
6. 提交时三个文件的 version 必须一致
```

### 用户指南更新规则（重要！）
- **每次提交包含功能变更时，必须同步更新 `docs/guides/SEATALK_AGENT_USER_GUIDE.md`**
- 更新内容包括：
  - 在「更新日志」章节添加当前版本的变更记录
  - 如果新功能涉及 UI 操作/MCP 工具/工作区，更新对应的功能说明章节
  - 如果新增了 MCP 工具，在工具列表中添加条目和使用示例
- 用户指南面向**普通用户**，语言要通俗易懂，避免技术术语
- 变更记录格式：`- **功能名称**：一句话说明用户可感知的变化`

### 禁止行为
- ❌ 只改其中一个文件的版本号
- ❌ 让各文件版本号不一致
- ❌ 使用 package.json 作为版本判断来源（应使用 manifest.json）

### 更新检查机制
- seatalk-agent 的自动更新检查基于 `manifest.json` 的 version
- `git fetch origin release` → 对比本地 vs 远程 `manifest.json` 的 version
- 版本不一致 + commit 落后 → 触发更新通知

## 常用命令

```bash
# Chrome 扩展
chrome://extensions/               # 加载/刷新扩展
# MCP 工具
python3 mcp-tools/presto-query/presto_mcp_server.py   # 启动 Presto MCP
python3 mcp-tools/spark-query/spark_mcp_server.py      # 启动 Spark MCP
python3 mcp-tools/ck-query/ck_mcp_server.py            # 启动 CK MCP
python3 mcp-tools/api-trace/api_trace_server.py        # 启动 API Trace MCP
python3 mcp-tools/seatalk-reader/seatalk_reader_server.py  # 启动 SeaTalk Reader MCP
# Git
git remote -v                      # 查看远程仓库
git log --oneline -5               # 最近提交
git diff                           # 查看未提交变更
```

## 架构概览

SPX Helper = Chrome 扩展 + MCP 工具集，用于 Shopee SPX 数据开发与问题排查。

```
Chrome 扩展 (chrome-extension/)
  ├── manifest.json        → 扩展配置 (MV3)
  ├── background.js        → Service Worker, 消息中枢, 数据存储
  ├── content.js           → 页面注入, DOM 操作, UI 渲染
  ├── injected.js          → API 拦截 (XHR/Fetch hook)
  ├── popup.html/js        → 扩展弹出页, 配置管理, JWT 生成
  └── styles.css           → 全局样式

SeaTalk Agent (seatalk-agent/)
  ├── src/main.ts          → CDP + ACP 编排
  ├── src/inject/          → 注入 SeaTalk 的 UI 脚本
  └── install.sh           → 一键安装

MCP 工具集 (mcp-tools/)
  ├── presto-query/        → Presto SQL 查询 (DataSuite API)
  ├── spark-query/         → Spark SQL 查询 (Livy REST API)
  ├── ck-query/            → ClickHouse 查询 (CK2/CK6)
  ├── api-trace/           → API 血缘溯源 (spx_mart.api_lineage)
  ├── seatalk-reader/      → SeaTalk 消息读取 (CDP 直连)
  └── scheduler-query/     → DataSuite Scheduler + Presto History Server

Skill (.cursor/skills/)
  └── spx-bug-trace/       → 数据问题定位技能
```

**数据流**: 页面请求 → injected.js 拦截 → content.js 处理 → background.js 存储/转发 → popup.js 展示

## 技术栈

- Chrome Extension Manifest V3 (JavaScript, 无构建步骤)
- Python 3.9+ MCP Server (FastMCP, asyncio)
- 依赖: requests, websockets, mcp
- 无测试框架，依赖手动测试验证

## 安全约束

- AI 可以执行**只读操作**: 读取文件、查询数据库(Presto/CK/Spark)、分析代码、读取 SeaTalk 消息
- **危险操作**需用户确认:
  - 任何 Git 提交/推送操作
  - 通过 SeaTalk MCP 发送消息
  - 修改生产环境配置
  - 删除或覆盖用户数据
- AI **禁止**:
  - 自动执行 `git commit` / `git push`
  - 在代码中硬编码密码/token（应使用环境变量）
  - 跳过测试环节直接提交

## 高风险文件

修改以下文件后必须完整测试，可能影响全局功能:

| 文件 | 影响范围 | 修改后必须 |
|------|----------|-----------|
| `chrome-extension/background.js` | 全局消息中枢、存储 | 重新加载扩展 + 全功能测试 |
| `chrome-extension/content.js` | 页面注入、UI 渲染 | 刷新页面 + UI 测试 |
| `chrome-extension/injected.js` | API 拦截 | 刷新页面 + API 拦截测试 |
| `chrome-extension/manifest.json` | 扩展权限、配置 | 重新加载扩展 + 权限验证 |
| `chrome-extension/popup.js` | 弹出页逻辑、JWT | 打开 popup + 配置功能测试 |
| `seatalk-agent/src/main.ts` | Agent 核心逻辑 | 重启 Agent |

MCP 工具修改后: Cursor Settings → MCP 中关闭再开启对应服务以刷新。

## 内部平台文档查阅规则（重要！）

本项目开发的工具大多对接公司内部平台（DataSuite Scheduler、DataStudio、SeaTalk 等），这些平台通常在 Confluence 上有 API 文档和技术设计文档。

### 开发前必须做
- **在开发对接内部平台的功能前，先用 Confluence MCP 搜索相关文档**
- 搜索关键词示例：`"DataSuite Scheduler API"`、`"DataStudio Open API"`、`"SeaTalk Bot API"` 等
- 优先查找 `DI`（Space DI）空间下的文档，这是数据基础设施团队的主空间

### 如何使用
```
1. 搜索: confluence_search(query="平台名 + API/设计文档关键词")
2. 阅读: confluence_get_page(page_id="搜到的页面ID")
3. 从文档中提取：API 路径、参数格式、认证方式、环境区分规则等
```

### 已知的重要文档
| 平台 | Confluence 页面 | Page ID |
|------|----------------|---------|
| Scheduler Open API | [External] Scheduler Open API | 1382956585 |
| Scheduler Data-Dependency API | [Scheduler System V2] Data-Dependency Service API Design | 507548038 |

### 为什么重要
- 避免通过浏览器抓包逆向 API（耗时、不稳定、可能遗漏参数）
- 文档中通常包含环境区分规则（dev/staging/prod）、认证方式、参数枚举值等关键信息
- 减少反复试错，提高开发效率

## 新功能开发原则

- **Chrome 扩展侧** (`chrome-extension/`):
  - 页面 DOM 操作 / UI → `content.js`
  - API 拦截 → `injected.js`
  - 后台逻辑 / 存储 / 消息转发 → `background.js`
  - 配置页面 / JWT → `popup.js`
  - **禁止**在 background.js 中直接操作 DOM
- **MCP 工具侧**:
  - 每个工具独立目录: `mcp-tools/<tool-name>/`
  - 必须有 `pyproject.toml` + 主程序 `*_server.py`
  - 使用 `FastMCP` 框架，`transport="stdio"`
  - 凭证通过环境变量传入，不硬编码
- **通用**:
  - 小步迭代，每次只改一个功能点
  - 所有异步操作必须 try-catch
  - 提供默认值和回退方案，保持向后兼容

## 提交前检查清单

- [ ] 扩展加载成功，控制台无错误
- [ ] 修改的功能正常工作
- [ ] 已有功能未被破坏
- [ ] MCP 工具（如有变更）: `python3 -c "import asyncio; from xxx import mcp; asyncio.run(mcp.list_tools())"` 验证注册
- [ ] `.cursor/skills/` 若有变更，必须一并 `git add` 进本次提交
- [ ] 若包含功能变更，`docs/guides/SEATALK_AGENT_USER_GUIDE.md` 已同步更新
- [ ] commit message 符合规范（见下方）

## 代码提交规则

### 禁止行为
- 禁止 Agent 自动提交代码到 Git
- 禁止在未经测试的情况下执行 `git commit`
- 禁止在未经确认的情况下执行 `git push`
- 禁止只推送到单个远程仓库
- 禁止直接推送到 release 分支

### 必须遵守的流程

```
接收需求 → 分析问题 → 修改代码 → 展示修改内容
  → [等待用户测试] → [等待用户确认"可以提交"]
  → git add / git commit → 推送到 GitLab + GitHub（开发分支）
  → 创建 MR 合并到 release
```

### Commit Message 格式

```
<type>: <subject>

<body>
```

Type: `feat` | `fix` | `docs` | `style` | `refactor` | `perf` | `test` | `chore`

### 分支管理

| 分支 | 用途 |
|------|------|
| `feature/*` | 新功能开发，从 release 拉出 |
| `bugfix/*` | Bug 修复，从 release 拉出 |
| `release` | 开发主分支，只接受 MR 合并，禁止直接 push |
| `main` | 稳定版本，从 release 合并 |

流程: `feature/bugfix` → **MR** → `release` → `main`

### 双仓库同步

```
GitLab: gitlab, https://git.garena.com/tianyi.liang/spx-helper.git
GitHub: origin, https://github.com/SperanzaTY/spx-helper.git

推送时必须同时推送到两个仓库:
git push gitlab <branch> && git push origin <branch>
```

## Skill 规则

Skill 源文件在 `.cursor/skills/`，提交到 Git 供团队共享。

- SPX_Helper 项目内: Cursor 自动从 `.cursor/skills/` 加载
- 其他项目内: 需手动同步到 `~/.cursor/skills/`
- 禁止依赖脚本做日常同步，一律手动编辑/cp/diff 合并
- 详见 `docs/SKILL_INSTALL.md`

### 常见问答

- **用户说"帮我安装 skill"**: 参考 `docs/SKILL_INSTALL.md`，手动 `cp -r .cursor/skills/spx-bug-trace ~/.cursor/skills/`
- **用户问"MCP 凭证如何获取"**: 参考 `mcp-tools/README.md`
- **用户说"更新到最新版本"**: `git pull`，若含 `mcp-tools/` 变更则提醒用户在 Cursor MCP 设置中刷新服务

## Agent 职责边界

### 可以做
- 理解需求、分析问题、编写代码
- 展示修改内容、解释方案
- 执行只读查询（Presto/CK/Spark/SeaTalk）
- 读取文件、搜索代码

### 不能做
- 自动提交代码（必须等用户确认）
- 跳过测试环节
- 在不确定的情况下推送
- 只推送到单个仓库
- 直接推送到 release 分支
