# SPX Helper 项目开发规则

## 🚫 代码提交规则（重要！）

### 禁止行为
- ❌ **禁止 Agent 自动提交代码到 Git**
- ❌ 禁止在未经测试的情况下执行 `git commit`
- ❌ 禁止在未经确认的情况下执行 `git push`
- ❌ 禁止批量修改多个文件后一次性提交
- ❌ 禁止只推送到单个远程仓库

### 必须遵守的流程

#### 1. 代码修改阶段
```
✅ 允许：
- 修改代码文件
- 创建新文件
- 删除文件
- 重构代码

❌ 禁止：
- 直接提交到 Git
```

#### 2. 测试阶段（必须！）
```
在提交前必须：
1. 在浏览器中加载扩展
2. 测试修改的功能
3. 验证没有破坏现有功能
4. 检查控制台是否有错误
5. 确认用户体验正常
```

#### 3. 提交阶段（需人工确认）
```
Agent 必须：
1. 向用户展示所有修改内容（git diff）
2. 说明修改的目的和影响范围
3. 等待用户确认测试完成
4. 等待用户明确指令后才能提交
5. 【Skill】若 .cursor/skills/ 有变更，必须一并 add 进本次提交

用户确认后才能执行：
- git add（包含 .cursor/skills/ 若有变更）
- git commit
- git push gitlab <当前分支>  ⚠️ 必须推送到 GitLab
- git push origin <当前分支>  ⚠️ 必须推送到 GitHub

**禁止直接提交到 release**。必须在 feature/bugfix 分支开发，通过 MR 合并到 release。
```

### 分支管理规则（重要！）

| 分支 | 用途 |
|------|------|
| **feature/*** | 新功能开发，从 release 拉出 |
| **bugfix/*** | Bug 修复，从 release 拉出 |
| **release** | 开发主分支，**只接受 MR 合并**，禁止直接 push |
| **main** | 稳定版本，从 release 合并，生产环境 |

流程：`feature/bugfix` → **MR** → `release` → `main`

- ❌ **禁止**：直接在 release 分支上 commit 并 push
- ✅ **必须**：在 feature/xxx 或 bugfix/xxx 分支开发 → 推送分支 → 创建 MR 合并到 release
- MR 地址（GitLab）：https://git.garena.com/tianyi.liang/spx-helper/-/merge_requests/new
- 完整流程见 [docs/GIT_WORKFLOW.md](docs/GIT_WORKFLOW.md)

### 双仓库同步规则（重要！）

```
本项目维护两个远程仓库：
- gitlab: https://git.garena.com/tianyi.liang/spx-helper.git
- origin: https://github.com/SperanzaTY/spx-helper.git

提交时必须：
1. ✅ 同时推送到两个仓库
2. ✅ 使用相同的 commit
3. ✅ 确保两个仓库保持同步

命令示例：
git push gitlab feature/xxx && git push origin feature/xxx   # 推送开发分支，再创建 MR
git push gitlab release && git push origin release          # 仅 MR 合并后由合并者推送 release
git push gitlab main && git push origin main                # 稳定版发布
```

### 正确的工作流程

```
┌─────────────────────────────────────────┐
│ 1. 接收需求                              │
│    ↓                                     │
│ 2. 分析问题                              │
│    ↓                                     │
│ 3. 修改代码                              │
│    ↓                                     │
│ 4. 展示修改内容                          │
│    ↓                                     │
│ 5. 🛑 等待用户测试                       │
│    ↓                                     │
│ 6. 🛑 等待用户确认："可以提交"           │
│    ↓                                     │
│ 7. 执行提交 (git add/commit)            │
│    ↓                                     │
│ 8. 推送到 GitLab + GitHub（开发分支）   │
│    ↓                                     │
│ 9. 创建 MR 合并到 release               │
│    ↓                                     │
│ 10. （可选）执行 sync_skills.sh 同步到本地 │
└─────────────────────────────────────────┘
```

## 📋 提交消息规范

### Commit Message 格式
```
<type>: <subject>

<body>

<footer>
```

### Type 类型
- `feat`: 新功能
- `fix`: 修复bug
- `docs`: 文档更新
- `style`: 代码格式调整（不影响功能）
- `refactor`: 重构（既不是新功能也不是修复）
- `perf`: 性能优化
- `test`: 测试相关
- `chore`: 构建/工具相关

### 示例
```bash
feat: 添加API请求参数展示功能

在API详情页面新增可折叠的请求参数区域。

- 使用details标签实现折叠
- JSON格式化展示
- 深色主题统一风格
```

## 🧪 测试清单

### Chrome 扩展测试
- [ ] 扩展加载成功
- [ ] 功能正常工作
- [ ] 控制台无错误
- [ ] 已有功能未被破坏
- [ ] UI显示正常
- [ ] 性能无明显下降

### API 相关功能测试
- [ ] API拦截正常
- [ ] 数据溯源功能正常
- [ ] AI分析功能正常
- [ ] 接口血缘查询正常

### 配置相关测试
- [ ] 配置保存成功
- [ ] 配置读取正常
- [ ] 默认值生效
- [ ] 导入导出功能正常

### 提交后验证
- [ ] GitLab 仓库已更新
- [ ] GitHub 仓库已更新
- [ ] 两个仓库的提交历史一致
- [ ] （可选）Skill 同步：执行 `./scripts/sync_skills.sh` 查看说明，在 Cursor 中比较后手动合并
## 📚 Skill 规则（团队同步，仅手动合并）

Skill 源文件在 `.cursor/skills/`，提交到 Git 供团队共享。

- **SPX_Helper 项目内**：Cursor 自动从 `.cursor/skills/` 加载
- **其他项目内**：需同步到 `~/.cursor/skills/` 才能加载
- **禁止自动覆盖**：不在脚本中做任何 cp/覆盖，只通过 Cursor 比较后手动修改
- **合并流程**：在 Cursor 中比较 `.cursor/skills/` 与 `~/.cursor/skills/`，手动复制更优内容到目标位置

### 用户说「帮我安装 skill」时

参考 `docs/SKILL_INSTALL.md`，执行 `./scripts/sync_skills.sh`：首次安装会自动复制到 `~/.cursor/skills/`；若已存在则不覆盖，提示在 Cursor 中比较合并。

### 用户问「Spark DMP 账号密码在哪里找」「某某 MCP 凭证如何获取」时

参考 `mcp-tools/README.md` 凭证获取指引：Spark 在 Data Suite 个人中心 → Profile；Presto 在 Data Suite API 管理 → 获取 Personal Token。

### 用户说「更新到最新版本」或「更新 release」时

1. 执行 `git pull origin release`（或 `git pull gitlab release`）
2. 检查本次拉取是否含 `mcp-tools/` 变更：`git diff ORIG_HEAD HEAD --name-only | grep mcp-tools`
3. **若有 MCP 变更**，必须提醒用户：
   > ⚠️ MCP 有更新，请到 Cursor 设置（Settings → MCP）中关闭再开启相应 MCP 服务以刷新，否则新版本不会生效。
4. 若包含 `.cursor/skills/` 变更，可提示执行 `./scripts/sync_skills.sh`

## 🔧 调试建议

### 查看日志
```javascript
// 在控制台查看 SPX Helper 的日志
// 使用过滤器：[SPX Helper]
```

### 重新加载扩展
```
1. 打开 chrome://extensions/
2. 点击"刷新"按钮
3. 刷新测试页面
```

### 检查远程仓库状态
```bash
# 查看远程仓库
git remote -v

# 查看分支状态
git status

# 查看提交历史
git log --oneline -5
```

## ⚠️ 风险操作

### 高风险修改（需要特别小心）
- background.js：影响全局功能
- content.js：影响页面注入
- manifest.json：影响扩展权限
- injected.js：影响API拦截

### 修改这些文件后必须：
1. 完整测试所有功能
2. 检查是否破坏现有功能
3. 验证性能影响
4. 确认无安全问题

## 📝 开发最佳实践

### 代码修改
1. **小步迭代**：每次只修改一个功能点
2. **保持向后兼容**：提供默认值和回退方案
3. **错误处理**：所有异步操作都要有 try-catch
4. **日志输出**：关键步骤输出日志便于调试

### 提交策略
1. **原子提交**：每次提交只包含一个逻辑修改
2. **清晰描述**：说明修改原因、方案和影响
3. **测试验证**：提交前必须测试
4. **双仓库同步**：确保 GitLab 和 GitHub 同步

### 回滚准备
1. 保持 Git 历史清晰
2. 记录每次修改的原因
3. 重要修改前创建分支
4. 两个仓库保持一致

## 🎯 总结

### Agent 的职责
✅ 理解需求
✅ 分析问题
✅ 编写代码
✅ 展示修改
✅ 解释方案
✅ 同时推送到两个仓库

### Agent 不应该做的
❌ 自动提交代码
❌ 跳过测试环节
❌ 在不确定的情况下推送
❌ 修改后不通知用户
❌ 只推送到单个仓库
❌ 直接推送到 release 分支（必须先 feature/bugfix → MR）

### 用户的职责
✅ 测试功能
✅ 确认修改正确
✅ 批准提交
✅ 提供反馈

## 📦 远程仓库与分支

```
GitLab: gitlab, https://git.garena.com/tianyi.liang/spx-helper.git
GitHub: origin, https://github.com/SperanzaTY/spx-helper.git

分支：
- feature/*, bugfix/*: 开发分支，通过 MR 合并到 release
- release:   开发主分支，仅接受 MR 合并
- main:      稳定版本
```

---

**记住：代码提交是不可逆的操作，必须慎重！**

**在用户明确说"可以提交"或"提交代码"之前，不要执行任何 Git 提交操作！**

**提交时必须同时推送到 GitLab 和 GitHub！**
