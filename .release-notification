📦 SPX Helper v3.6.6 文档已更新

本次不升级版本号，继续保持 v3.6.6。

这次同步了 `spx-bug-trace` 的本地沉淀到仓库里的 Codex 入口：
- repo-scoped skill 新增 `references/sinks.md`
- 明确本地过程文档命名规则：`docs/investigations/YYYYMMDD-<short-description>.md`
- 明确 Confluence 标题前缀必须使用 `FM-` / `MM-` / `LM-`
- 明确 Google Sheets `app问题整理` / `坑点` 的写回约束与 service account 要求

这样 Codex 读精简版 `spx-bug-trace` 时，也能直接拿到这些命名和沉淀硬规则。
