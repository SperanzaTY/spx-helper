📦 SPX Helper v3.6.5 已发布

本次更新新增 repo-scoped skill `mcp-capability-build`，专门用于在 SPX Helper 里构建新的 MCP 能力。

这套方法论现在明确覆盖：
- 用户只给页面或链接时，先做 capability mining，挖掘可实现点
- 用 CDP 或真实接口证实页面能力，而不是凭页面文案猜接口
- 复用 chrome-auth，包括 cookie_db + cdp_cookie、静默 SSO 刷新、401/403 重试与认证诊断
- 探查后先给用户候选能力清单，再引导选择优先级
- 继续完成实现、真环境 smoke，以及正式 release/publish

相关文档已同步到 docs/guides/SKILL.md。
