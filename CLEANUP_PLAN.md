# Git 分支清理计划

## 🔍 当前问题分析

### 分支状态

```bash
main (被污染)
├── v2.13.0 - v2.14.0 提交
└── 最新: 210b706 docs: 添加v2.14.0清理更新说明

release (稳定版本)
├── v2.10.0
└── 最新: 71fea09 chore: 发布 v2.10.0

bugfix/text-selection-disable (当前分支)
├── 包含 bug 修复
└── 从 main 创建（错误，应该从 release 创建）
```

### 问题根源

1. ❌ 错误理解：以为 main 是开发分支
2. ❌ 直接提交：大量未测试代码提交到 main
3. ❌ 分支错误：bugfix 从 main 创建而非 release

---

## ✅ 正确理解（已修正）

### 分支角色

- **release** = 开发分支（包含每次变更）
- **main** = 稳定版本（经过测试，可发布）

### 正确工作流

```
1. 从 release 创建 feature/bugfix 分支
2. 开发完成，合并回 release
3. 在 release 充分测试
4. 测试通过，release → main
5. 在 main 打 tag 发布
```

---

## 🛠️ 清理方案

### 方案 A：完全重置（推荐，干净）

**目标**: main 回到 v2.10.0，所有新功能在 release 测试

```bash
# Step 1: 回滚 main 到 release 的稳定版本
git checkout main
git reset --hard release  # main 回到 v2.10.0
git push origin main --force

# Step 2: 将所有新功能合并到 release
git checkout release
git merge main  # 已经同步了，没有变化

# 现在把 bugfix 的修复也加入
git merge bugfix/text-selection-disable

# 如果需要新功能，从 release 创建新分支重新开发
git checkout -b feature/api-tracker-enhancements

# 把之前的功能重新实现并测试
# ... 

# 合并回 release
git checkout release
git merge feature/api-tracker-enhancements

# Step 3: 充分测试 release 分支

# Step 4: 测试通过后，release → main
git checkout main
git merge release
git tag -a v2.14.0 -m "Stable release v2.14.0"
git push origin main --tags
```

### 方案 B：增量修复（简单，但历史不干净）

**目标**: 承认 main 已包含新功能，在 release 测试后追认

```bash
# Step 1: 将 main 的所有变更合并到 release
git checkout release
git merge main  # 这会把 v2.13-v2.14 都合并过来

# Step 2: 合并 bugfix
git merge bugfix/text-selection-disable
git branch -d bugfix/text-selection-disable

# Step 3: 在 release 充分测试所有功能
# 使用测试清单逐项验证

# Step 4: 测试通过后，确认 main 稳定
# 如果发现问题，在 release 修复
# 修复后，release → main

# Step 5: 测试完全通过
git checkout main
git merge release  # 同步 bugfix
git tag -a v2.14.1 -m "Stable release v2.14.1"
git push origin main --tags
```

---

## 📊 方案对比

| 方案 | 优点 | 缺点 | 适用场景 |
|------|------|------|----------|
| **方案 A** | ✅ 历史干净<br>✅ 流程规范 | ❌ 需要重新整理<br>❌ 费时费力 | 追求完美，时间充足 |
| **方案 B** | ✅ 操作简单<br>✅ 快速恢复 | ❌ main 历史不纯<br>❌ 妥协方案 | 快速修复，继续开发 |

---

## 🎯 我的建议

**推荐方案 B**，理由：

1. 新功能（v2.13-v2.14）虽然未测试，但功能完整
2. 只需修复已知 bug（文本选取关闭后弹窗）
3. 在 release 集中测试所有功能
4. 测试通过后追认为稳定版本

**流程：**
```
1. 测试 bugfix → 合并到 release
2. 测试 release 所有功能
3. 测试通过 → release → main
4. 打 tag v2.14.1 发布
```

---

## 📝 当前待办

### 🔴 紧急

1. **你测试 bugfix**
   - 重新加载扩展
   - 关闭文本选取功能
   - 选择文字，确认不弹窗

### 🟡 中等

2. **选择清理方案**
   - 方案 A: 彻底清理
   - 方案 B: 快速修复

3. **执行清理**
   - 我根据你的选择执行相应操作

### 🟢 后续

4. **建立规范**
   - 以后严格遵守 Git 工作流
   - feature/bugfix → release → 测试 → main

---

## 💭 反思

**我的错误：**
1. 理解错误：以为 main 是开发分支
2. 未经测试：直接提交大量代码
3. 流程混乱：不同分支角色不清

**改进措施：**
1. ✅ 已创建 GIT_WORKFLOW.md 规范
2. ✅ 以后所有开发用 feature/bugfix 分支
3. ✅ 只在 bugfix 分支工作，等你测试通过
4. ✅ 绝不再直接提交 main

---

**现在等你测试和决定清理方案！** 🙏
